# 対戦ビンゴゲーム 詳細仕様書（現在版）

---

## 🎯 最重要：登場人物と視点

```
┌─────────────────────────────────────────────────────┐
│  このゲームには2人の登場人物がいる                    │
│                                                     │
│  【プレイヤー】= 人間 = キミ / あなた = 画面の前のユーザー │
│  【ルナ】= AI = CPU = わたくし = セリフを喋る側        │
│                                                     │
│  ⚠️ セリフは全てルナ視点で書く！！！                  │
└─────────────────────────────────────────────────────┘
```

ルナの一人称は「わたくし」、プレイヤーの呼び方はゲーム内メッセージでは「あなた」、
ボード見出しなど固定UI文言では「キミ」を使用（例：「🎮 キミ のボード」）。

---

## 🎲 ゲームの基本ルール

### ボードについて
- プレイヤーとルナ、それぞれが5×5のボードを持つ
- 各ボードには1～25の数字がランダムに配置される
- 両者のボードの数字配置は異なる

### マークについて
```
【超重要】誰が選んだ数字が、誰のボードにどうマークされるか

┌────────────────────────────────────────────────────────────┐
│  プレイヤーが数字Xを選んだ場合：                            │
│    → プレイヤーのボード：Xの位置に × マーク（selfMarks）    │
│    → ルナのボード：Xの位置に ○ マーク（selfMarks）         │
│                                                            │
│  ルナが数字Yを選んだ場合：                                  │
│    → プレイヤーのボード：Yの位置に ○ マーク（opponentMarks）│
│    → ルナのボード：Yの位置に × マーク（opponentMarks）      │
└────────────────────────────────────────────────────────────┘

つまり：
  - selfMarks = プレイヤーが選んだ数字の集合
  - opponentMarks = ルナが選んだ数字の集合
  
表示記号：
  - プレイヤーのボード：○ = ルナの選択、× = プレイヤーの選択
  - ルナのボード（デバッグ表示）：○ = プレイヤーの選択、× = ルナの選択
```

### 勝利条件
```
【プレイヤーの勝利】
  プレイヤーのボード上で ○（opponentMarks）が5個揃う
  = ルナが選んだ数字でプレイヤーのビンゴが完成
  
【ルナの勝利】
  ルナのボード上で ○（selfMarks）が5個揃う
  = プレイヤーが選んだ数字でルナのビンゴが完成
```

---

## 🎰 じゃんけんフェーズ

### ルール
- 負けた方が先に数字を選ぶ
- 勝った方が後攻（有利）
- じゃんけん中はボード（キミ・ルナとも）を一切表示しない。表示されるのはトグルスイッチと手のボタンのみ

### 表示方法
```
あいこの場合：
キミ: ✊ 🔺 vs うち: ✌️ 🔺

キミが勝った場合（ルナが負けた）：
キミ: ✊ ⭕️ vs うち: ✌️ ❌

うちが勝った場合（キミが負けた）：
キミ: ✊ ❌ vs うち: ✌️ ⭕️
```

### ボタンの即時非表示
- 手（✊✌️✋）をクリックした瞬間、`jankenPlayerChoice` がセットされるため、3つの手ボタンは即座に非表示になる
- 表示条件：`currentTurn === 'janken' && jankenPlayerChoice === null`

### あいこの再挑戦とラウンド表示
- あいこの場合、1秒後（`setTimeout(..., 1000)`）に `jankenPlayerChoice` / `jankenCpuChoice` をクリアし、手ボタンを再表示
- 同時に `jankenRound` を+1する
- ターン表示は `jankenRound` が2以上のとき「じゃんけん（2回目）」「じゃんけん（3回目）」のように回数を表示（1回目は単に「じゃんけん」）
- 新しいゲーム（リセット）で `jankenRound` は1に戻る

### 処理フロー
```
プレイヤーがじゃんけんに勝った場合（ルナが負けた）：
  → ルナが先に選ぶ（currentTurn = 'cpu'）
  → メッセージ：jankenLose（ルナが負けて悔しいセリフ）
  → ボード初期化
  → 3秒後にcpuTurn()を呼ぶ

プレイヤーがじゃんけんに負けた場合（ルナが勝った）：
  → プレイヤーが先に選ぶ（currentTurn = 'player'）
  → メッセージ：jankenWin（ルナが勝って嬉しいセリフ）
  → ボード初期化

あいこの場合：
  → 1秒後にもう一回じゃんけん（jankenRound + 1）
  → メッセージ：jankenDraw（あいこのセリフ）
```

### オープン／クローズモードの切り替え
- じゃんけん画面の手ボタンの上に、トグルスイッチ（デフォルト：クローズ）を表示
- クローズ：ルナのボードは非表示のまま進行
- オープン：じゃんけん成立後、ルナのボードも表示される
- トグルの下に説明文「クローズ：ルナのボードは見えませんの／オープン：ルナのボードも見えますわ」を表示
- 状態は `boardMode`（`'open' | 'closed'`）で管理。新しいゲームで `'closed'` にリセット

---

## 🔢 数字選択フェーズ

### ターン表示
```
ターン履歴を色分けで表示（ターン1から全て）
例：8 5 12 7 3 ターン6：キミ

色分け：
  - 🔵 青 = プレイヤー（キミ）
  - 🔴 赤 = ルナ
```

### handleNumberClick関数の処理

#### ルナが数字を選んだ場合（isCpuMove = true）

```javascript
// 1. opponentMarksに追加（ルナの選択を記録）
const newOpponentMarks = new Set(opponentMarks);
newOpponentMarks.add(number);
setOpponentMarks(newOpponentMarks);
setTurnHistory(prev => [...prev, { player: 'cpu', number }]);

// 2. プレイヤーのボードでリーチ/ビンゴ判定
//    ⚠️ 超重要：newOpponentMarksを渡す！！！
const playerBoardCheck = checkLinesForNumber(
  playerBoard,        // プレイヤーのボード
  newOpponentMarks,   // ⭐ ルナの選択（○マーク）をカウント
  number,
  newUsedNumbers
);

// 3. 結果に応じた処理
if (playerBoardCheck.hasWin) {
  // プレイヤーがビンゴ → プレイヤーの勝利
  setGameStatus('player-win');
  setMessage(getRandomMessage('playerWin'));
}

if (playerBoardCheck.hasReach && !reachMode) {
  // プレイヤーがリーチ → ルナが追加ターン
  setPlayerReach(true);
  setReachMode('player-reach');
  setMessage(getRandomMessage('playerReach'));
  // ルナがもう1回選ぶ
}
```

#### プレイヤーが数字を選んだ場合（isCpuMove = false）

```javascript
// 1. selfMarksに追加（プレイヤーの選択を記録）
const newSelfMarks = new Set(selfMarks);
newSelfMarks.add(number);
setSelfMarks(newSelfMarks);
setTurnHistory(prev => [...prev, { player: 'player', number }]);

// 2. ルナのボードでリーチ/ビンゴ判定
//    ⚠️ 超重要：newSelfMarksを渡す！！！
const cpuBoardCheck = checkLinesForNumber(
  cpuBoard,           // ルナのボード
  newSelfMarks,       // ⭐ プレイヤーの選択（○マーク）をカウント
  number,
  newUsedNumbers
);

// 3. 結果に応じた処理
if (cpuBoardCheck.hasWin) {
  // ルナがビンゴ → ルナの勝利
  setGameStatus('luna-win');
  setMessage(getRandomMessage('lunaWin'));
}

if (cpuBoardCheck.hasReach && !reachMode) {
  // ルナがリーチ → プレイヤーが追加ターン
  setCpuReach(true);
  setReachMode('luna-reach');
  setMessage(getRandomMessage('lunaReach'));
  // プレイヤーがもう1回選ぶ
}
```

---

## 🎯 リーチシステム

### リーチとは
- あるボード上で○マークが4つ揃い、残り1つで勝利の状態
- リーチになった側は「地雷原を進む」立場になる

### リーチ発動条件

```
┌─────────────────────────────────────────────────────────────┐
│ 【プレイヤーがリーチ】                                       │
│   発動条件：プレイヤーのボード上で○（opponentMarks）が4つ揃う │
│   発動タイミング：ルナが数字を選んだ時                        │
│   結果：ルナがもう1回選ぶ                                      │
│   reachMode：'player-reach'                                  │
│   ボタン表示：「ルナ、もう1回」（緑色・小サイズ）             │
│   メッセージ：playerReach（ルナがピンチで焦るセリフ）         │
├─────────────────────────────────────────────────────────────┤
│ 【ルナがリーチ】                                             │
│   発動条件：ルナのボード上で○（selfMarks）が4つ揃う          │
│   発動タイミング：プレイヤーが数字を選んだ時                  │
│   結果：プレイヤーがもう1回選ぶ                               │
│   reachMode：'luna-reach'                                    │
│   ボタン表示：「キミ、もう1回です」（赤色・小サイズ）         │
│   メッセージ：lunaReach（ルナがリーチで嬉しいセリフ）         │
└─────────────────────────────────────────────────────────────┘
```

### リーチ線の表示

```
⚠️ 超重要：リーチ線の表示ルール

プレイヤーのボード（メイン表示）：
  - playerReachLines を使って黄色で強調表示
  - プレイヤーがリーチの時だけ光る
  - ルナがリーチの時は何も表示しない！

ルナのボード（デバッグ用）：
  - リーチ線の強調表示は不要（空配列を渡す）
```

### ターン進行
**重要：リーチでもう1回選んでもターンは進む！**
```
ターン1：キミ 8
ターン2：ルナ 5
ターン3：キミ 12 （プレイヤーがリーチ）
ターン4：ルナ 7  （ルナが追加ターン）← ターン番号進む
ターン5：ルナ 3  （ルナが追加ターン）← ターン番号進む
ターン6：キミ ... （ターン制御戻る）
```

---

## 💬 メッセージシステム

### 絶対ルール
```
┌─────────────────────────────────────────────────────────────┐
│  全てのメッセージはルナ視点で書く！！！                       │
│                                                             │
│  ルナは一人称「わたくし」を使う                              │
│  プレイヤーのことは「あなた」と呼ぶ                          │
└─────────────────────────────────────────────────────────────┘
```

### 口調ルール（自然で丁寧・フレンドリーな女性口調）
```
一人称は「わたし」、プレイヤーは「あなた」と呼ぶ（固定UI文言では「キミ」）。

使う語尾：
  - 「〜です」「〜ますね」「〜ましょう」「〜ですね」
  - 丁寧語ベースだが、堅苦しくなりすぎない自然な話し言葉

使わない語尾・表現：
  - 「〜ですわ」「〜ですの」「わたくし」「〜くださいまし」← 旧お嬢様設定（廃止）
  - 「〜なんよ」「〜だけど」「うち」← さらに旧いキャラ設定（廃止）
  - 「ふふ」「ふふふ」「女神」など砕けた煽り表現は使わない
```

各メッセージカテゴリ（jankenStart / jankenWin / jankenLose / jankenDraw / start /
draw / playerWin / lunaWin / lunaSelect / playerReach / lunaReach / lunaTurn /
playerTurnAgain）は、`getRandomMessage(type, number)` 内の配列からランダムに1つ選ばれる。
各カテゴリ8〜14種程度のバリエーションを持たせ、セリフの単調さを避けている。

### ルール説明ポップアップの口調（メッセージとは別トーン）
「📋 ルール」ボタンで開くポップアップは、ルナのセリフとは切り離し、
淡々とした業務的な文体（「〜します」「〜となります」）で統一する。
箇条書きの記号も「✨」ではなく「・」を使用する。
戦略のコツ（先攻不利・中央マスの価値など）も同じ箇条書きの最後に含める。

---

## 🤖 CPU（ルナ）の数字選択戦略

### 基本原則
**相手のボードは見えない。自分のボード上の情報だけで判定する。**

### 関数名
`getDefensiveMove` - 防御的な手を選択する戦略関数

### スコア判定ロジック

各選択可能な数字について、自分のボード上の4本のライン（縦・横・斜め2本）をチェック：

```javascript
for (const line of lines) {
  const opponentCount = line.filter(n => opponentMarks.has(n)).length;
  // opponentMarks = 相手が選んだ数字（自分のボード上で○が着いてる）
  
  if (opponentCount === 4) {
    // 相手の○が4個揃ってるラインにこのマスがある
    // = ここに×をつけると自分のビンゴが完成できなくなる
    scoreMap[num] = -1000; // 絶対避ける
  } else {
    // 相手の○が多いほど低スコア（置きたくない）
    score -= opponentCount;
  }
}

// 斜めのラインで相手の○が0個
// = 「斜めでいけるかも」なラインを潰したくない
if (isDiagonal && opponentCount === 0) {
  score -= 1; // さらに低く
}
```

### スコア判定の意味
```
相手の○が4個 → 置くな（スコア -1000）
相手の○が3個 → 置きたくない（スコア -3）
相手の○が2個 → 置きたくない（スコア -2）
相手の○が1個 → 置きたくない（スコア -1）
相手の○が0個（縦・横） → スコア 0（普通）
相手の○が0個（斜め） → スコア -1（真ん中狙いたいから避ける）

最後に：スコアが最も高いマスを選ぶ
```

### 人間の「おまかせ」ボタンも同じロジック
プレイヤーが「おまかせ」をクリックした場合、**同じ戦略で自動選択される**

---

## 🎨 UI・表示

### タイトル行
```
🌟 対 戦 ビ ン ゴ 🌟
（各文字が異なる色：紫→ピンク→赤→オレンジ→黄色）
（星がビョンビョン演出あり／見出しフォントは next/font/google の M PLUS Rounded 1c）
```
- じゃんけん画面：`text-xl sm:text-2xl md:text-4xl`（当初の`text-3xl/4xl/6xl`から縮小）
- プレイ中（数字選択フェーズ以降）：PC（md以上）では`md:text-4xl`でそのまま表示、
  スマホ幅（md未満）では **完全に非表示**（`hidden md:block`）にしてボードを上に詰める

### ボード表示・レイアウト（PC・スマホ共通の1カラム構成）
PCの2カラム横並びグリッドや、スマホでルナのボードを右上に重ねて乗せる旧レイアウトは廃止し、
PC・スマホともに同じ縦積み構成にした。

```
【オープンモード時（boardMode === 'open'）】
  1. Luna行（幅98%、md:max-w-lg で上限）
     ┌───────────────┬─────────────────────────┐
     │ ルナのボード   │ ルナのメッセージエリア    │
     │ （幅40%）      │ （幅58%）                │
     │ compact表示    │ 白背景カード、上下中央    │
     │                │ 揃えでメッセージを表示、  │
     │                │ 下部に選択番号履歴を表示  │
     └───────────────┴─────────────────────────┘
     （以前試した `-mb-3` 等のマイナスマージンによる
      キミのボードへの重なり演出は廃止し、通常の
      `mb-1.5 sm:mb-2` の余白で完全に非重複にした）
  2. キミのボード（max-w-xs〜md、中央寄せ）
     その直下（ボードの枠の外）に選択番号履歴は出さない
     （オープン時は履歴はLuna行のメッセージ欄側に表示）

【クローズモード時】
  1. キミのボードのみ中央表示
  2. その直下（ボードの枠の外）に選択番号履歴を表示
```
- ルナのボードは常に`renderLunaBoard(true)`（compact版）のみを使用。PC専用のフルサイズ表示は廃止した
- プレイヤーのボード見出し：「🎮 キミ のボード」、背景は白
- ルナのボード見出し（compact）：「🐛 ルナ」、背景はアンバー（bg-amber-50、border-amber-200）

### 選択番号履歴（`historyEl`）
- ターン表示バーとは完全に分離した独立要素
- タイトル「選択番号履歴:」＋ 番号を色分け表示（🔵青=キミ、🔴赤=ルナ）
- オープンモード時：Luna行内のメッセージエリアの下部に表示
- クローズモード時：キミのボードの直下、ボードカードの枠の外に表示

### リセット（強制終了）ボタン・ルールボタン
- どちらも画面**最下部**の横並びバー（`w-[98%] md:max-w-lg mx-auto`、`justify-between`）に配置
- 左：リセット（強制終了）ボタン（グレー、通常の丸ボタン。以前あったボードへの「乗る」配置は廃止）
- 右：📋 ルールボタン（小さめの丸ボタン）
- クリックで `initializeBoard()` ＋ 各種状態リセット（`jankenRound` を1に、`boardMode` を `'closed'` に）

### ルールの表示（ポップアップモーダル）
- 常設パネルではなく、「📋 ルール」ボタンを押すと開くモーダルポップアップ（`rulesModalOpen`）
- 背景を暗くしたオーバーレイの中央にカード表示。カード外クリック or ×ボタンで閉じる
- 開閉状態は完全に手動。じゃんけん成立後も自動では開閉しない
- ゲーム終了時に出す「終了（次のゲームへ）」ボタンは、通常時の「もう1回」ボタン群と同じ位置（中央のボタン列）に表示され、押すと`initializeBoard()`で新しいゲームへ

### ターン情報表示
```
【場所】：メッセージ欄の上のバー（w-[98%] md:max-w-lg mx-auto）
【形式】：現在のターン情報 + おまかせボタン（右寄せ、ml-auto）
         ※ ターン履歴の数字はこのバーには含まれず、historyElとして分離済み

じゃんけん中のターン表示は「じゃんけん」、あいこが続くと
「じゃんけん（2回目）」「じゃんけん（3回目）」のように回数が増える
```

### メッセージ欄
- 通常時（クローズモード、またはじゃんけん中）：`max-w-xl`（PC）/ `max-w-[85%]`（スマホ）で中央寄せ
- オープンモード時：Luna行内のメッセージエリア（幅58%）内に表示され、上下中央揃え
- テキストは改行対応（whitespace-pre-line）

### ボタン配置
```
【じゃんけん時】：トグルスイッチ（オープン/クローズ）＋ 中央に3個（✊ ✌️ ✋）
                 各ボタンは px-4 py-1.5 md:px-5 md:py-2、text-xl md:text-2xl で細め
                 選択直後、手ボタンは即座に非表示になる
【リーチ時】：中央に小サイズのボタン（「ルナ、もう1回」/「キミ、もう1回です」）
【終了時】：中央に「終了（次のゲームへ）」ボタン（紫）
【通常時】：おまかせボタンはターン表示バーの右に配置（ml-auto、常に右寄せ）
```

### おまかせボタン
- アイコン：Wand2（魔法の杖）✨
- 色：青
- サイズ：小（text-sm）
- 位置：ターン表示バーの右（`ml-auto`でturnHistoryの折り返しに関わらず常に右寄せ）

---

## 🔧 State一覧

```javascript
// ボード
playerBoard     // プレイヤーの5×5ボード
cpuBoard        // ルナの5×5ボード

// マーク（超重要！）
selfMarks       // プレイヤーが選んだ数字のSet
opponentMarks   // ルナが選んだ数字のSet
usedNumbers     // 使用済み数字のSet（両方の合計）

// ゲーム状態
currentTurn     // 'janken' | 'player' | 'cpu'
gameStatus      // 'playing' | 'player-win' | 'luna-win'

// リーチ状態
playerReach     // プレイヤーがリーチか（boolean）
cpuReach        // ルナがリーチか（boolean）
reachMode       // null | 'player-reach' | 'luna-reach'

// リーチ線情報
playerReachLines  // プレイヤーのリーチ線（表示用）
cpuReachLines     // ルナのリーチ線（使わない）

// ビンゴ線情報
playerWinLines    // プレイヤーのビンゴ線（表示用）
cpuWinLines       // ルナのビンゴ線（使わない）

// ターン履歴
turnHistory     // ターン情報の配列 [{ player: 'player'|'cpu', number: N }, ...]

// ボード公開モード
boardMode       // 'open' | 'closed'（デフォルト 'closed'）

// じゃんけんラウンド
jankenRound     // あいこのたびに+1（表示：「じゃんけん（N回目）」、新ゲームで1に戻る）

// ルール表示
rulesModalOpen  // ルールポップアップの開閉（boolean、デフォルトfalse。手動トグルのみ）
```

---

## 📊 処理フロー図

```
【ゲーム開始】
    │
    ▼
┌─────────────┐
│ じゃんけん   │
└─────────────┘
    │
    ├─── ルナ勝ち → プレイヤー先攻
    │
    └─── ルナ負け → ルナ先攻（3秒後にcpuTurn）
    │
    ▼
┌─────────────────────────────────────────┐
│              メインループ                 │
│                                          │
│  ┌─────────────┐    ┌─────────────┐     │
│  │プレイヤー選択│ ←→ │ ルナ選択    │     │
│  └─────────────┘    └─────────────┘     │
│         │                  │             │
│         ▼                  ▼             │
│  ┌─────────────┐    ┌─────────────┐     │
│  │cpuBoardで   │    │playerBoardで│     │
│  │判定         │    │判定         │     │
│  │(selfMarks)  │    │(opponentMarks)    │
│  └─────────────┘    └─────────────┘     │
│         │                  │             │
│    ┌────┴────┐        ┌────┴────┐       │
│    ▼         ▼        ▼         ▼       │
│ ルナ勝利  ルナリーチ プレイヤー プレイヤー│
│           ↓         リーチ     勝利      │
│        プレイヤー    ↓                   │
│        追加ターン  ルナ追加ターン         │
│        （ターン進む）（ターン進む）       │
└─────────────────────────────────────────┘
```

---

## ✅ 実装チェックリスト

- [x] initializeBoardでsetGameStatus('playing')を呼んでいる
- [x] ルナ選択時はnewOpponentMarksでplayerBoardを判定
- [x] プレイヤー選択時はnewSelfMarksでcpuBoardを判定
- [x] playerWinメッセージはルナが負けて悔しい内容
- [x] lunaWinメッセージはルナが勝って嬉しい内容
- [x] playerReachメッセージはルナがピンチで焦る内容
- [x] lunaReachメッセージはルナがリーチで嬉しい内容
- [x] リーチ線表示はplayerBoardでplayerReachLinesのみ使用
- [x] 口調は「です」「ますね」中心の自然で丁寧・フレンドリーな女性口調。「ですわ」「ですの」「わたくし」「うち」「なんよ」「ふふ」は使わない
- [x] じゃんけん表示：⭕️（勝ち）、❌（負け）、🔺（あいこ）
- [x] じゃんけん中はボードを表示しない
- [x] じゃんけんの手ボタンは選択直後に即座に非表示、あいこ時は1秒後に再表示
- [x] あいこの回数は「じゃんけん（N回目）」で表示
- [x] ルナがリーチ時のボタン：「キミ、もう1回です」（小サイズ）
- [x] プレイヤーがリーチ時のボタン：「ルナ、もう1回」（小サイズ）
- [x] CPU戦略：相手のボードは見ない。自分のボードだけ見る
- [x] リーチでもう1回選んでもターン番号は進む
- [x] ターン履歴：色分け表示（青=キミ、赤=ルナ）
- [x] おまかせボタン：Wand2アイコン、ターン表示の右寄せ
- [x] setTurnHistoryは関数形式（prev =>）で使用
- [x] じゃんけんのあいこは`jankenDraw`メッセージを使用（`draw`ではない）
- [x] オープン/クローズのモード切り替えはトグルスイッチ（デフォルト：クローズ）
- [x] ルナのボードはPC・スマホともにcompact表示（幅40%）、メッセージエリア（幅58%）と横並び
- [x] リセット（強制終了）ボタンとルールボタンは画面最下部の横並びバーに配置（左：リセット、右：ルール）
- [x] ルールは「📋 ルール」ボタンで開くポップアップモーダル（常設パネルではない）
- [x] ルールポップアップの文体はメッセージと切り離し、業務的な「です・ます」調＋「・」箇条書き
- [x] 選択番号履歴（historyEl）はターン表示バーとは別要素として分離表示
- [x] Luna行（ルナのボード＋メッセージエリア）とキミのボードは重ならない（マイナスマージンでの重なり演出は廃止）
- [x] playerBoard/cpuBoardのmapには必ずkey属性を設定
- [x] 戦略関数名は`getDefensiveMove`（`getSmartMove`ではない）

---

**この仕様書に従えば、完璧な対戦ビンゴゲーム！！！**

---

## 📝 修正履歴

### 2025-12-30 修正版
以下の修正を実施：

1. **じゃんけんのあいこメッセージ修正**（ゲームロジック）
   - `draw` → `jankenDraw` に変更
   - これによりじゃんけんのあいこが正しく動作

2. **語尾の統一**（メッセージテキスト）
   - 「なんですけど」→「なんよ」「なんだけど」に修正
   - ルナのキャラ設定を統一

3. **ルール説明の視点修正**（表示テキスト）
   - 「あたし」→「キミ」「うち」に修正
   - ルナ視点で統一

4. **関数名変更**（コード品質）
   - `getSmartMove` → `getDefensiveMove` に変更
   - 防御的戦略であることを明確化

5. **UI修正**
   - ボードタイトル：「🐛 ルナ のボード（デバッグ）」→「🐛 ルナ（デバッグ）」
   - レイアウト：横並び（flex）を明確化
   - Reactのkey属性の重要性を追記

### 2026-07-24 修正版
実装を Vite → Next.js（静的エクスポート）に移行済みの状態から、以下を大幅改修：

1. **キャラクター口調の全面刷新**
   - 一人称「うち」→「わたくし」、語尾「なんよ」→「ですわ／ですの」に変更
   - 当初は元気ハイテンション寄りのお嬢様口調にしたが、その後「真面目寄り」の
     落ち着いたお嬢様口調へ再調整（「ふふ」「女神」等の煽り表現を削除）
   - ルールパネルはメッセージと切り離し、業務的な「です・ます」調に統一

2. **ボード公開モード（オープン／クローズ）の追加**
   - 常時表示されていたルナのデバッグボードを、プレイヤーが選べるトグルに変更
   - デフォルトはクローズ（非公開）。じゃんけん画面の手ボタンの上にトグルを配置
   - オープン時、PCでは同サイズ横並び、スマホではプレイヤーのボード幅の35%で
     右上（見出しに軽く乗る形）に表示

3. **じゃんけんフェーズのUX改善**
   - じゃんけん中はボードを完全非表示にしてボードとの情報の混在を防止
   - 手ボタンを選択直後に即座に非表示、あいこ時は1秒後に再表示
   - あいこの回数を「じゃんけん（2回目）」のように表示

4. **リセット動線の変更**
   - 独立した「新しいゲーム」ボタンを廃止
   - 「リセット（強制終了）」という小さいボタンに変更し、
     プレイヤーのボード左上の角に乗る形で常設

5. **リーチボタンの縮小**
   - 「むむっ、ルナもう１回…」「ふふっ、キミもう１回です！」という大きな煽り文言を
     「ルナ、もう1回」「キミ、もう1回です」に短縮し、ボタンサイズも縮小

6. **レスポンシブ対応**
   - スマホ幅では2つのボードを縦に並び替え、プレイ中はタイトルを非表示にして
     ボードを上に詰める
   - メッセージ欄に最大幅を設定し、PC・スマホともに横に広がりすぎないよう調整

### 2026-07-24 追加修正
上記からさらに、以下を改修：

1. **口調の再調整（お嬢様口調 → 自然で丁寧・フレンドリーな女性口調）**
   - 「ですわ」「ですの」「わたくし」を廃止し、「です」「ますね」中心の
     自然な話し言葉に変更。一人称は「わたし」

2. **ルール表示をトグル式パネル → ポップアップモーダルに変更**
   - 常設だったルールパネルを廃止し、「📋 ルール」ボタンを押したときだけ
     開くモーダルに変更（`rulesModalOpen`、手動開閉のみ）
   - ルール箇条書きの末尾に、じゃんけんの先攻・後攻の説明と、
     「先攻はやや不利／中央など多くのライン上のマスは価値が高い」という
     戦略のコツを追記

3. **レイアウトの全面刷新（PC・スマホ共通の1カラム構成に統一）**
   - PCの2カラム横並びグリッドと、スマホでルナのボードを右上に重ねて乗せる
     旧レイアウトを廃止
   - ルナのボード（幅40%）とルナのメッセージエリア（幅58%）を横並びにした
     「Luna行」（幅98%、`md:max-w-lg`で上限）を新設。ルナのボードは常に
     compact表示に統一（フルサイズ表示は廃止）
   - 選択番号履歴（`historyEl`）をターン表示バーから独立した要素として分離。
     オープン時はLuna行のメッセージエリア下部、クローズ時はキミのボード
     直下（ボードの枠の外）に表示
   - リセット（強制終了）ボタンはボードへの「乗る」配置をやめ、ルールボタンと
     並べて画面最下部の横並びバーに移動（左：リセット、右：ルール）
   - Luna行とキミのボードが重なる演出（マイナスマージン）を一時的に試したが、
     最終的に重ならない通常の余白に戻した

4. **タイトルのフォントサイズ縮小**
   - 🌟対戦ビンゴ🌟の見出しをひとまわり小さく調整
     （じゃんけん時：`text-3xl/4xl/6xl` → `text-xl/2xl/4xl`、
     プレイ中PC表示：`md:text-6xl` → `md:text-4xl`）

5. **CPU（ルナ）AIの公平性を確認**
   - `getDefensiveMove`は自分のボードと公開情報（使用済み数字・双方のマーク）
     のみを参照しており、相手のボードレイアウトは一切参照しないことをコード
     レビューで確認済み（不正なし）
   - このゲームは学校の「対戦型ビンゴ」レク（EDUPEDIA等で紹介）に相当する
     ルール構成であることを確認

